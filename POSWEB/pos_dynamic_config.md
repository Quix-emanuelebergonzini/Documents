pos_dynamic_config serve per sovvrascrivere il config.py in tutti i negozi di un posweblite
una certa chiave con un determinato valore.

un metodo risolutivo e veloce perché altrimenti sarebbe tedioso